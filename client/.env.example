# VitaCore client — environment sample
# Copy to client/.env and adjust.

# Base URL of the VitaCore API
VITE_API_URL=http://localhost:5000/api

# "true"  => connect to the live Express/Mongo API
# "false" => run fully standalone in demo mode (localStorage), no backend required
VITE_USE_API=false
